\documentclass[10pt,a4paper,ragged2e]{./altacv}

\typeout{*** JOBNAME = \jobname ***}
\typeout{*** \string\ifcolorful = \ifcolorful TRUE\else FALSE\fi ***}

\usepackage{multicol}
\usepackage{ragged2e}
\usepackage{etoolbox}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{enumitem}
\usepackage{fontspec}

\defaultfontfeatures{%
  Ligatures=TeX,%
  FallbackFamily={NotoSans-Regular}%
}

\makeatletter
\renewcommand{\makecvheader}{%
  \begingroup
    \altacv@left@photos\hfill%
    \begin{minipage}{\dimexpr\linewidth-\altacv@photos@width}%
      \raggedright%
      {\namefont\color{name}\MakeUppercase{\@name}\par}%
      \smallskip%
      {\taglinefont\color{tagline}\@tagline\par}%
      \smallskip%
      {\personalinfofont\@personalinfo\par}%
    \end{minipage}\hfill%
    \altacv@right@photos\par\smallskip%
  \endgroup
}

\renewcommand{\cvsection}[2][]{%
  \par\medskip
  \if\relax\detokenize{#1}\relax
  \else
    \marginpar{\raggedright\scriptsize\itshape #1\par}%
  \fi
  \noindent
    {\RaggedRight\color{heading}\cvsectionfont\MakeUppercase{#2}\par}%
  \vspace{-1ex}%
  \noindent{\color{headingrule}\rule{\linewidth}{2pt}}\par\smallskip
}

\renewcommand{\cvevent}[4]{%
  \noindent
    {\large\color{emphasis}#1}%
  \hfill
    \ifstrequal{#2}{}{}{{\color{black}#2}}%
  \\[-0.0ex]%
    \ifstrequal{#3}{}{}{{\small\faCalendar\hspace{0.5em}#3}}%
  \hfill
    \ifstrequal{#4}{}{}{{\small\faMapMarker\hspace{0.5em}#4}}%
  \vspace{0.0ex}%
}

\renewcommand{\divider}{%
  \par\smallskip
  \centering
  {\color{body!30}\hdashrule{\linewidth}{0.5pt}{0.2ex}}%
  \par\smallskip
}

\newcommand{\eduline}[1]{%
  \par\noindent\RaggedRight#1\par%
  \addvspace{-\bigskipamount}
  \smallskip
}

\renewcommand{\itemmarker}{{\small\textbullet}}
\renewcommand{\ratingmarker}{\faCircle}

\makeatother

\setlist[itemize]{%
  label=\itemmarker,
  left=0pt,
  before=\RaggedRight,
  after=\justifying
}

\geometry{
  left=0.5cm,
  right=0.5cm,
  top=0.5cm,
  bottom=0.5cm,
}

\setmainfont{NotoSans_ExtraCondensed}[
  Path            =./fonts/,
  Extension       =.ttf,
  UprightFont     = *-Regular,
  ItalicFont      = *-Italic,
  BoldFont        = *-Bold,
  BoldItalicFont  = *-BoldItalic,
  SemiBoldFont    = *-SemiBold,
  ExtraBoldFont   = *-ExtraBold,
  BlackFont       = *-Black,
  MediumFont      = *-Medium,
  LightFont       = *-Light,
  ExtraLightFont  = *-ExtraLight,
  ThinFont        = *-Thin,
  Scale           = 1.2
]

\newfontfamily\quotefont{NotoSansMono-Regular}[
  Path      =./fonts/,
  Extension =.ttf,
  Scale     = 0.75
]

\AtBeginEnvironment{itemize}{\small}

\setlength{\columnsep}{0.5cm}

\definecolor{Accent}{HTML}{007fff}
\definecolor{Gray}{HTML}{4f4f4f}
\definecolor{Black}{HTML}{000000}
\colorlet{heading}{Black}
\colorlet{accent}{Black}
\colorlet{emphasis}{Black}
\colorlet{tagline}{Black}
\colorlet{body}{Black}

\RenewEnviron{quote}{%
  \begingroup
    \color{black}%
    \quotefont\large
    \BODY%
   \par
  \endgroup
}

\name{Emalyn Lindeman}
\tagline{Administrative \& Program Support Specialist}
\personalinfo{%
  \mailaddress{emalyn@lndmn.com}\\%
  \phone{(623) 313-4703}\\%
  \linkedin{/emalyn-lindeman-41a050206}\\%
  \location{Utica, NY}%
}

\newif\ifcolorful%
\colorfulfalse%
\ifdefined\colorful%
  \colorfultrue%
\fi

\ifcolorful%
  \definecolor{Accent}{HTML}{880c59}
  \colorlet{heading}{Accent}
  \colorlet{accent}{Accent}
  \colorlet{tagline}{Accent}
  \colorlet{body}{Gray}
\fi

\AtBeginDocument{%
  \hyphenpenalty=200
  \exhyphenpenalty=100
  \emergencystretch=1.5em
  \noindent%
  \begin{minipage}[t]{\linewidth}%
    \begin{minipage}[t]{13cm}%
      \ifcolorful%
        \begin{tikzpicture}[baseline=(current bounding box.center)]%
          \clip (0,0) circle (1.25cm);%
          \node at (0,0){\includegraphics[width=2.5cm]{Emalyn}};%
          \draw[accent,line width=0.4pt] (0,0) circle (1.25cm);%
        \end{tikzpicture}\hspace{5mm}%
      \fi%
      \vtop{\makecvheader}%
    \end{minipage}%
  \end{minipage}%
  \par\medskip%
  \noindent\begin{minipage}[t]{\linewidth}%
    \cvsection{Summary}%
    \begin{quote}\noindent%
      Administrative and program support professional with direct experience in documentation, data tracking, and multi-system coordination. Managed records and progress notes for hundreds of students across concurrent programs; built original systems from scratch with no template or prior framework. Comfortable working independently in a remote setting, handling diverse workloads with accuracy and a high level of professionalism. Drawn to mission-driven organizations where organized, detail-oriented support makes a real difference for families.
    \end{quote}%
  \end{minipage}%
}
